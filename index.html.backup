<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>IPL Auction 2026 - Multiplayer</title>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
<style>
* { margin: 0; padding: 0; box-sizing: border-box; }

:root {
  --bg-dark: #0a0e1a;
  --bg-card: #151b2e;
  --bg-card-light: #1d2539;
  --primary: #3b82f6;
  --primary-dark: #2563eb;
  --accent: #fbbf24;
  --success: #10b981;
  --danger: #ef4444;
  --text: #f3f4f6;
  --text-dim: #9ca3af;
  --text-dimmer: #6b7280;
  --border: rgba(255,255,255,0.1);
}

body {
  font-family: 'Inter', sans-serif;
  background: linear-gradient(135deg, var(--bg-dark) 0%, #1a1f35 100%);
  color: var(--text);
  min-height: 100vh;
}

.container { max-width: 1400px; margin: 0 auto; padding: 20px; }

.screen { display: none; }
.screen.active { display: block; }

/* HOME SCREEN */
.home-screen {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 20px;
}

.home-card {
  background: var(--bg-card);
  border-radius: 20px;
  padding: 40px;
  width: 100%;
  max-width: 500px;
  border: 1px solid var(--border);
  box-shadow: 0 20px 60px rgba(0,0,0,0.4);
}

.logo {
  text-align: center;
  margin-bottom: 32px;
}

.logo h1 {
  font-size: 2.5rem;
  font-weight: 900;
  background: linear-gradient(135deg, var(--primary), var(--accent));
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  margin-bottom: 8px;
}

.logo p {
  color: var(--text-dim);
  font-size: 0.95rem;
}

.form-group {
  margin-bottom: 20px;
}

.form-group label {
  display: block;
  margin-bottom: 8px;
  font-weight: 600;
  font-size: 0.9rem;
  color: var(--text-dim);
}

.form-group input, .form-group select {
  width: 100%;
  padding: 14px 16px;
  background: var(--bg-card-light);
  border: 2px solid var(--border);
  border-radius: 12px;
  color: var(--text);
  font-size: 1rem;
  font-family: 'Inter';
  transition: all 0.3s;
}

.form-group input:focus, .form-group select:focus {
  outline: none;
  border-color: var(--primary);
  box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
}

.teams-grid {
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  gap: 10px;
}

.team-btn {
  padding: 12px 8px;
  background: var(--bg-card-light);
  border: 2px solid var(--border);
  border-radius: 12px;
  cursor: pointer;
  transition: all 0.3s;
  text-align: center;
}

.team-btn:hover {
  transform: translateY(-2px);
  border-color: rgba(255,255,255,0.2);
}

.team-btn.selected {
  border-color: var(--primary);
  background: rgba(59, 130, 246, 0.1);
  box-shadow: 0 0 20px rgba(59, 130, 246, 0.2);
}

.team-btn.disabled {
  opacity: 0.3;
  cursor: not-allowed;
}

.team-logo {
  width: 32px;
  height: 32px;
  border-radius: 50%;
  margin: 0 auto 6px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 0.7rem;
  font-weight: 800;
}

.team-name {
  font-size: 0.7rem;
  font-weight: 600;
}

.btn {
  width: 100%;
  padding: 16px;
  background: linear-gradient(135deg, var(--primary), var(--primary-dark));
  color: white;
  border: none;
  border-radius: 12px;
  font-size: 1.1rem;
  font-weight: 700;
  cursor: pointer;
  transition: all 0.3s;
  font-family: 'Inter';
  margin-top: 10px;
}

.btn:hover:not(:disabled) {
  transform: translateY(-2px);
  box-shadow: 0 10px 30px rgba(59, 130, 246, 0.3);
}

.btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.btn-secondary {
  background: var(--bg-card-light);
  border: 2px solid var(--border);
}

.btn-secondary:hover:not(:disabled) {
  border-color: var(--primary);
  box-shadow: 0 10px 30px rgba(59, 130, 246, 0.2);
}

.divider {
  text-align: center;
  margin: 24px 0;
  color: var(--text-dim);
  font-size: 0.9rem;
}

/* LOBBY SCREEN */
.lobby-screen {
  min-height: 100vh;
  padding: 40px 20px;
}

.lobby-container {
  max-width: 800px;
  margin: 0 auto;
}

.lobby-header {
  background: var(--bg-card);
  border-radius: 16px;
  padding: 24px;
  margin-bottom: 24px;
  border: 1px solid var(--border);
  text-align: center;
}

.room-code-display {
  background: var(--bg-card-light);
  padding: 16px;
  border-radius: 12px;
  margin: 16px 0;
  display: inline-block;
}

.room-code-label {
  font-size: 0.8rem;
  color: var(--text-dim);
  margin-bottom: 4px;
}

.room-code {
  font-size: 2rem;
  font-weight: 900;
  color: var(--accent);
  letter-spacing: 4px;
}

.share-btn {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 10px 20px;
  background: var(--bg-card-light);
  border: 2px solid var(--border);
  border-radius: 10px;
  color: var(--text);
  cursor: pointer;
  transition: all 0.3s;
  font-family: 'Inter';
  font-weight: 600;
}

.share-btn:hover {
  border-color: var(--success);
  color: var(--success);
}

.players-section {
  background: var(--bg-card);
  border-radius: 16px;
  padding: 24px;
  margin-bottom: 24px;
  border: 1px solid var(--border);
}

.section-title {
  font-size: 1.1rem;
  font-weight: 700;
  margin-bottom: 16px;
  color: var(--text-dim);
}

.player-card {
  background: var(--bg-card-light);
  padding: 16px;
  border-radius: 12px;
  margin-bottom: 12px;
  display: flex;
  align-items: center;
  gap: 16px;
  border: 2px solid var(--border);
}

.player-card.host {
  border-color: var(--accent);
  box-shadow: 0 0 20px rgba(251, 191, 36, 0.2);
}

.player-badge {
  width: 48px;
  height: 48px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 800;
  font-size: 0.8rem;
  flex-shrink: 0;
}

.player-info {
  flex: 1;
}

.player-info-name {
  font-size: 1rem;
  font-weight: 700;
}

.player-info-team {
  font-size: 0.85rem;
  color: var(--text-dim);
}

.host-badge {
  background: var(--accent);
  color: var(--bg-dark);
  padding: 4px 12px;
  border-radius: 20px;
  font-size: 0.75rem;
  font-weight: 700;
}

.waiting-players {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12px;
  padding: 32px;
  color: var(--text-dim);
}

.loader {
  width: 16px;
  height: 16px;
  border: 2px solid var(--border);
  border-top-color: var(--primary);
  border-radius: 50%;
  animation: spin 0.8s linear infinite;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

/* AUCTION SCREEN */
.auction-header {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: 16px;
  padding: 16px 24px;
  margin-bottom: 20px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  flex-wrap: wrap;
  gap: 16px;
}

.header-left {
  display: flex;
  align-items: center;
  gap: 16px;
}

.user-info {
  display: flex;
  align-items: center;
  gap: 12px;
}

.user-team-badge {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 800;
  font-size: 0.75rem;
}

.user-details h3 {
  font-size: 1rem;
  font-weight: 700;
}

.user-details p {
  font-size: 0.8rem;
  color: var(--text-dim);
}

.header-stats {
  display: flex;
  gap: 24px;
}

.stat {
  text-align: center;
}

.stat-label {
  font-size: 0.75rem;
  color: var(--text-dim);
  margin-bottom: 4px;
}

.stat-value {
  font-size: 1.1rem;
  font-weight: 700;
  color: var(--success);
}

.auction-controls {
  display: flex;
  gap: 12px;
}

.control-btn {
  padding: 10px 20px;
  border: none;
  border-radius: 10px;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s;
  font-family: 'Inter';
}

.control-btn.pause {
  background: var(--accent);
  color: var(--bg-dark);
}

.control-btn.end {
  background: var(--danger);
  color: white;
}

.control-btn:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(0,0,0,0.3);
}

/* AUCTION GRID */
.auction-grid {
  display: grid;
  grid-template-columns: 300px 1fr 340px;
  gap: 20px;
  align-items: start;
}

.upcoming-panel, .teams-panel {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: 16px;
  padding: 20px;
  max-height: calc(100vh - 160px);
  overflow-y: auto;
  position: sticky;
  top: 20px;
}

.upcoming-panel h3, .teams-panel h3 {
  font-size: 0.9rem;
  font-weight: 700;
  color: var(--text-dim);
  margin-bottom: 16px;
  text-transform: uppercase;
  letter-spacing: 1px;
}

.upcoming-list {
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.upcoming-player {
  background: var(--bg-card-light);
  padding: 12px;
  border-radius: 10px;
  border: 1px solid var(--border);
}

.upcoming-player.current {
  border-color: var(--accent);
  box-shadow: 0 0 15px rgba(251, 191, 36, 0.2);
}

.up-name {
  font-size: 0.85rem;
  font-weight: 600;
  margin-bottom: 4px;
}

.up-role {
  font-size: 0.7rem;
  color: var(--text-dim);
  display: inline-block;
  padding: 2px 8px;
  background: rgba(255,255,255,0.05);
  border-radius: 4px;
}

.up-base {
  font-size: 0.75rem;
  color: var(--accent);
  font-weight: 600;
  margin-top: 6px;
}

/* PLAYER PANEL */
.player-panel {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: 16px;
  padding: 32px;
}

.current-player-header {
  display: flex;
  justify-content: space-between;
  align-items: start;
  margin-bottom: 24px;
}

.player-info-main {
  flex: 1;
}

.player-role-badge {
  display: inline-block;
  padding: 6px 14px;
  background: rgba(59, 130, 246, 0.1);
  border: 1px solid rgba(59, 130, 246, 0.3);
  border-radius: 20px;
  font-size: 0.75rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 1px;
  margin-bottom: 12px;
}

.player-role-badge.batsman { background: rgba(59, 130, 246, 0.1); border-color: rgba(59, 130, 246, 0.3); color: #60a5fa; }
.player-role-badge.all_rounder { background: rgba(168, 85, 247, 0.1); border-color: rgba(168, 85, 247, 0.3); color: #c084fc; }
.player-role-badge.bowler { background: rgba(239, 68, 68, 0.1); border-color: rgba(239, 68, 68, 0.3); color: #f87171; }

.player-name-large {
  font-size: 2rem;
  font-weight: 800;
  margin-bottom: 6px;
  line-height: 1.2;
}

.player-teams-text {
  font-size: 0.9rem;
  color: var(--text-dim);
}

.timer-circle {
  width: 100px;
  height: 100px;
  position: relative;
}

.timer-svg {
  transform: rotate(-90deg);
}

.timer-bg {
  fill: none;
  stroke: var(--bg-card-light);
  stroke-width: 8;
}

.timer-progress {
  fill: none;
  stroke: var(--accent);
  stroke-width: 8;
  stroke-linecap: round;
  transition: stroke-dashoffset 1s linear;
}

.timer-text {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  font-size: 2rem;
  font-weight: 800;
  color: var(--accent);
}

.base-price-display {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 10px 18px;
  background: rgba(251, 191, 36, 0.1);
  border: 1px solid rgba(251, 191, 36, 0.3);
  border-radius: 10px;
  margin: 20px 0;
}

.base-price-display span {
  font-size: 0.75rem;
  color: var(--text-dim);
}

.base-price-display strong {
  font-size: 1.1rem;
  color: var(--accent);
  font-weight: 700;
}

.stats-container {
  margin-top: 24px;
}

.stats-title {
  font-size: 0.8rem;
  font-weight: 700;
  color: var(--text-dim);
  margin-bottom: 12px;
  text-transform: uppercase;
  letter-spacing: 1px;
}

.stats-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(100px, 1fr));
  gap: 12px;
}

.stat-box {
  background: var(--bg-card-light);
  padding: 14px;
  border-radius: 10px;
  text-align: center;
  border: 1px solid var(--border);
}

.stat-box-value {
  font-size: 1.2rem;
  font-weight: 700;
  color: var(--text);
  margin-bottom: 4px;
}

.stat-box-label {
  font-size: 0.7rem;
  color: var(--text-dimmer);
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

/* BIDDING */
.bidding-section {
  margin-top: 32px;
  padding-top: 32px;
  border-top: 1px solid var(--border);
}

.current-bid {
  text-align: center;
  margin-bottom: 24px;
}

.current-bid-label {
  font-size: 0.85rem;
  color: var(--text-dim);
  margin-bottom: 6px;
}

.current-bid-amount {
  font-size: 3rem;
  font-weight: 900;
  color: var(--accent);
  margin-bottom: 6px;
}

.current-bidder {
  font-size: 0.95rem;
  color: var(--text-dim);
}

.current-bidder strong {
  color: var(--text);
  font-weight: 600;
}

.bid-controls {
  display: flex;
  gap: 12px;
  justify-content: center;
  margin-bottom: 20px;
}

.bid-btn {
  flex: 1;
  max-width: 200px;
  padding: 16px 24px;
  border: none;
  border-radius: 12px;
  font-size: 1.05rem;
  font-weight: 700;
  cursor: pointer;
  transition: all 0.3s;
  font-family: 'Inter';
}

.bid-btn-primary {
  background: linear-gradient(135deg, var(--success), #059669);
  color: white;
  box-shadow: 0 4px 20px rgba(16, 185, 129, 0.3);
}

.bid-btn-primary:hover:not(:disabled) {
  transform: translateY(-2px);
  box-shadow: 0 8px 30px rgba(16, 185, 129, 0.4);
}

.bid-btn:disabled {
  opacity: 0.4;
  cursor: not-allowed;
}

.bid-history {
  max-height: 150px;
  overflow-y: auto;
  background: var(--bg-card-light);
  border-radius: 10px;
  padding: 12px;
}

.bid-history-item {
  display: flex;
  justify-content: space-between;
  padding: 8px 0;
  border-bottom: 1px solid var(--border);
  font-size: 0.85rem;
}

.bid-history-item:last-child {
  border-bottom: none;
}

.bid-history-team {
  font-weight: 600;
}

.bid-history-amount {
  color: var(--accent);
  font-weight: 700;
}

/* TEAMS PANEL */
.team-card {
  background: var(--bg-card-light);
  padding: 14px;
  border-radius: 12px;
  border: 1px solid var(--border);
  margin-bottom: 10px;
}

.team-card.user-team {
  border-color: var(--primary);
  box-shadow: 0 0 20px rgba(59, 130, 246, 0.2);
}

.team-card.active-bidder {
  border-color: var(--success);
  background: rgba(16, 185, 129, 0.05);
}

.team-card-header {
  display: flex;
  align-items: center;
  gap: 10px;
  margin-bottom: 10px;
}

.team-card-logo {
  width: 32px;
  height: 32px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 0.65rem;
  font-weight: 800;
  flex-shrink: 0;
}

.team-card-info {
  flex: 1;
  min-width: 0;
}

.team-card-name {
  font-size: 0.85rem;
  font-weight: 700;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.team-card-purse {
  font-size: 0.75rem;
  color: var(--success);
  font-weight: 600;
}

.team-players-list {
  margin-top: 8px;
  padding-top: 8px;
  border-top: 1px solid var(--border);
}

.team-player-item {
  display: flex;
  justify-content: space-between;
  padding: 4px 0;
  font-size: 0.75rem;
}

.team-player-name {
  color: var(--text-dim);
  flex: 1;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.team-player-price {
  color: var(--accent);
  font-weight: 600;
  margin-left: 8px;
}

.no-players {
  font-size: 0.75rem;
  color: var(--text-dimmer);
  text-align: center;
  padding: 8px 0;
}

/* SOLD OVERLAY */
.sold-overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.9);
  display: none;
  align-items: center;
  justify-content: center;
  z-index: 1000;
  animation: fadeIn 0.3s;
}

.sold-overlay.show {
  display: flex;
}

.sold-content {
  text-align: center;
  animation: scaleIn 0.5s cubic-bezier(0.68, -0.55, 0.265, 1.55);
}

.sold-badge {
  font-size: 6rem;
  font-weight: 900;
  color: var(--success);
  text-shadow: 0 0 60px rgba(16, 185, 129, 0.5);
  margin-bottom: 20px;
}

.sold-badge.unsold {
  color: var(--text-dimmer);
}

.sold-player-name {
  font-size: 2rem;
  font-weight: 700;
  margin-bottom: 10px;
}

.sold-team {
  font-size: 1.3rem;
  color: var(--text-dim);
  margin-bottom: 10px;
}

.sold-price {
  font-size: 3rem;
  font-weight: 900;
  color: var(--accent);
}

@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

@keyframes scaleIn {
  from { transform: scale(0.3); opacity: 0; }
  to { transform: scale(1); opacity: 1; }
}

/* PAUSED OVERLAY */
.paused-overlay {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background: var(--bg-card);
  padding: 32px 48px;
  border-radius: 20px;
  border: 2px solid var(--accent);
  z-index: 999;
  text-align: center;
  display: none;
}

.paused-overlay.show {
  display: block;
}

.paused-text {
  font-size: 2rem;
  font-weight: 800;
  color: var(--accent);
}

@media (max-width: 1200px) {
  .auction-grid {
    grid-template-columns: 1fr;
  }
  
  .upcoming-panel, .teams-panel {
    position: relative;
    max-height: 400px;
  }
  
  .teams-grid {
    grid-template-columns: repeat(3, 1fr);
  }
}

@media (max-width: 600px) {
  .teams-grid {
    grid-template-columns: repeat(2, 1fr);
  }
  
  .player-name-large {
    font-size: 1.5rem;
  }
  
  .current-bid-amount {
    font-size: 2rem;
  }
}

::-webkit-scrollbar { width: 6px; }
::-webkit-scrollbar-track { background: transparent; }
::-webkit-scrollbar-thumb { background: rgba(255,255,255,0.2); border-radius: 4px; }
</style>
</head>
<body>

<!-- HOME SCREEN -->
<div id="homeScreen" class="screen active">
  <div class="home-screen">
    <div class="home-card">
      <div class="logo">
        <h1>IPL AUCTION 2026</h1>
        <p>Multiplayer Real-Time Bidding</p>
      </div>

      <div class="form-group">
        <label>Your Name</label>
        <input type="text" id="playerName" placeholder="Enter your name" maxlength="20">
      </div>

      <div class="form-group">
        <label>Choose Your Team</label>
        <div class="teams-grid" id="teamsGrid"></div>
      </div>

      <div id="createRoomSection">
        <div class="form-group">
          <label>Max Players (2-10)</label>
          <select id="maxPlayers">
            <option value="2">2 Players</option>
            <option value="3">3 Players</option>
            <option value="4">4 Players</option>
            <option value="5">5 Players</option>
            <option value="6">6 Players</option>
            <option value="7">7 Players</option>
            <option value="8">8 Players</option>
            <option value="9">9 Players</option>
            <option value="10" selected>10 Players</option>
          </select>
        </div>

        <div class="form-group">
          <label>Bidding Timer (5-30 seconds)</label>
          <select id="timerDuration">
            <option value="5">5 seconds</option>
            <option value="10">10 seconds</option>
            <option value="15" selected>15 seconds</option>
            <option value="20">20 seconds</option>
            <option value="25">25 seconds</option>
            <option value="30">30 seconds</option>
          </select>
        </div>

        <button class="btn" id="createRoomBtn" disabled>Create Room</button>
      </div>

      <div class="divider">OR</div>

      <div class="form-group">
        <label>Room Code</label>
        <input type="text" id="roomCodeInput" placeholder="Enter 6-digit code" maxlength="6">
      </div>

      <button class="btn btn-secondary" id="joinRoomBtn" disabled>Join Room</button>
    </div>
  </div>
</div>

<!-- LOBBY SCREEN -->
<div id="lobbyScreen" class="screen">
  <div class="lobby-screen">
    <div class="lobby-container">
      <div class="lobby-header">
        <h2>Auction Lobby</h2>
        <div class="room-code-display">
          <div class="room-code-label">Room Code</div>
          <div class="room-code" id="roomCodeDisplay">------</div>
        </div>
        <button class="share-btn" id="shareBtn">
          📋 Copy Room Code
        </button>
      </div>

      <div class="players-section">
        <div class="section-title">
          <span id="playerCount">0</span> / <span id="maxPlayerCount">10</span> Players
        </div>
        <div id="playersList"></div>
        <div id="waitingMessage" class="waiting-players" style="display: none;">
          <div class="loader"></div>
          <span>Waiting for more players to join...</span>
        </div>
      </div>

      <button class="btn" id="startAuctionBtn" disabled style="display: none;">Start Auction</button>
      <button class="btn btn-secondary" id="leaveRoomBtn">Leave Room</button>
    </div>
  </div>
</div>

<!-- AUCTION SCREEN -->
<div id="auctionScreen" class="screen">
  <div class="container">
    <div class="auction-header">
      <div class="header-left">
        <div class="user-info">
          <div class="user-team-badge" id="userBadge"></div>
          <div class="user-details">
            <h3 id="userNameDisplay">Player</h3>
            <p id="userTeamDisplay">Team</p>
          </div>
        </div>
      </div>
      <div class="header-stats">
        <div class="stat">
          <div class="stat-label">Purse Remaining</div>
          <div class="stat-value" id="userPurse">₹120 Cr</div>
        </div>
        <div class="stat">
          <div class="stat-label">Players Bought</div>
          <div class="stat-value" id="userCount">0</div>
        </div>
      </div>
      <div class="auction-controls">
        <button class="control-btn pause" id="pauseBtn">⏸ Pause</button>
        <button class="control-btn end" id="endAuctionBtn" style="display: none;">🛑 End Auction</button>
      </div>
    </div>

    <div class="auction-grid">
      <div class="upcoming-panel">
        <h3>Next Players</h3>
        <div class="upcoming-list" id="upcomingList"></div>
      </div>

      <div class="player-panel" id="playerPanel"></div>

      <div class="teams-panel">
        <h3>Teams & Squads</h3>
        <div id="teamsList"></div>
      </div>
    </div>
  </div>
</div>

<div class="sold-overlay" id="soldOverlay">
  <div class="sold-content" id="soldContent"></div>
</div>

<div class="paused-overlay" id="pausedOverlay">
  <div class="paused-text">⏸ AUCTION PAUSED</div>
</div>

<script src="auction.js"></script>
</body>
</html>
